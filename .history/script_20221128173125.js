const number = 13195;

function getMax(num) {
  // const arr = [];
  // for (let i = 2; i < Math.sqrt(num); i++) {
  //   if (num % i === 0) {
  //     arr.push(i);
  //   }
  // }
  // return arr;
  const arr = [];
  for (let i = 0; i < Math.sqrt)
}

console.log(getMax(number));